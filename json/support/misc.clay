substr(string, offset, length) {
    var r = String();
    for (i in range(offset, offset + length)) {
        r += string[i];
    }
    return r;
}

concat(..strings) {
    var r = String();
    ..for (s in strings) {
        push(r, s);
    }
    return r;
}
